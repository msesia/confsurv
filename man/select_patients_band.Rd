% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screening.R
\name{select_patients_band}
\alias{select_patients_band}
\title{Select Patients Based on Survival Bands}
\usage{
select_patients_band(
  time.points,
  survival_lb,
  survival_ub,
  screening_time,
  screening_prob,
  screening_crit
)
}
\arguments{
\item{time.points}{A numeric vector of time points corresponding to the survival curves.}

\item{survival_lb}{A matrix of lower survival bounds (patients × time points).}

\item{survival_ub}{A matrix of upper survival bounds (patients × time points).}

\item{screening_time}{A scalar time at which to apply the screening rule.}

\item{screening_prob}{A scalar survival probability threshold for screening.}

\item{screening_crit}{A string indicating the screening criterion. One of \code{"low risk"} (select if survival > threshold)
or \code{"high risk"} (select if survival < threshold).}
}
\value{
A list with:
\describe{
\item{time}{The screening time used.}
\item{selected}{An integer vector of patient indices who meet the screening criterion.}
}
}
\description{
Applies a screening rule to select patients whose conformal survival bands meet a specified risk threshold.
}
\details{
This function selects patients whose \strong{conformal survival band} at a given time satisfies a risk threshold.
Specifically, it uses the lower bound of the band if screening for "low risk", and the upper bound if screening for "high risk".

Internally, it delegates to \code{\link[=select_patients_point]{select_patients_point()}} for interpolation and comparison.
}
\seealso{
\code{\link[=select_patients_point]{select_patients_point()}}, \code{\link[=conformal_survival_band]{conformal_survival_band()}}
}
